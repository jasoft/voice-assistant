services:
  voice-assistant:
    build: .
    ports:
      - "10031:10031"  # ptt-api
      - "8880:8080"    # sqlite_web (容器8080 -> 主机8880)
    volumes:
      - ./data:/app/data  # 映射 data 目录下的所有文件和子目录
    restart: unless-stopped
