services:
  voice-assistant:
    container_name: voice-assistant-1
    build: .
    env_file: .env
    environment:
      - TZ=Asia/Shanghai
    ports:
      - "10031:10031"
      - "18090:18090"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
